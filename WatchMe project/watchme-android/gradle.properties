org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Optional local overrides for backend configuration. You can also set these as
# environment variables with the same names.
# WATCHME_DEBUG_API_BASE_URL=http://10.0.2.2:8787
# WATCHME_RELEASE_API_BASE_URL=https://api.example.com
#
# Optional release signing inputs. These can live in keystore.properties at the
# repo root or be provided as environment variables.
# WATCHME_UPLOAD_STORE_FILE=../secure/watchme-upload.jks
# WATCHME_UPLOAD_STORE_PASSWORD=changeit
# WATCHME_UPLOAD_KEY_ALIAS=watchme
# WATCHME_UPLOAD_KEY_PASSWORD=changeit
